{
  "name": "recording",
  "private": true,
  "type": "module",
  "description": "SiloCityLabs Recorder PWA — tests only; the site itself has no bundler.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "fake-indexeddb": "^6.0.1",
    "happy-dom": "^17.6.3",
    "vitest": "^3.2.4"
  }
}
